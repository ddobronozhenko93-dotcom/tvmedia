<resources>

    <style name="Theme.Leanback" parent="Theme.Leanback.Browse">
        <!-- Чёрный фон, без вспышек -->
        <item name="android:windowBackground">@android:color/black</item>

        <!-- Без лишних анимаций -->
        <item name="android:windowAnimationStyle">@null</item>
    </style>

</resources>

